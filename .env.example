# Generate this once with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
NOVAVISION_SECRET_TRANSPORT_KEY=replace-with-generated-fernet-key

# Example secret requested by the component
ENV_SECRET_TEST=replace-with-secret-value
